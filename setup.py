from setuptools import setup

setup(name='deepShit',
      version='0.1',
      description='',
      url='https://github.com/hackyhacker/deepShit.git',
      packages=['deepShit'],
      zip_safe=False,
      install_requires=[''],
      test_suite='py.test',
      tests_require=['pytest'])
